import { ManageStepperDirective } from './manage-stepper.directive';

describe('ManageStepperDirective', () => {
  it('should create an instance', () => {
    //const directive = new ManageStepperDirective();
    //expect(directive).toBeTruthy();
  });
});
