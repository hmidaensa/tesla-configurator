import { FormatUrlImgPipe } from './format-url-img.pipe';

describe('FormatUrlImgPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUrlImgPipe();
    expect(pipe).toBeTruthy();
  });
});
